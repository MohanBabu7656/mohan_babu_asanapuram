/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
  },
  devIndicators: {
    appIsrStatus: false,
    buildActivity: false,
  },
};

export default config;
